# 개발 커밋 히스토리 (원문)

이 프로젝트를 만들면서 쌓인 git 커밋 메시지를 시간순(오래된 것부터)으로
그대로 모아둔 기록입니다. 내용은 고치지 않고 원문 그대로 실었고,
읽기 편하게 날짜/커밋 해시로 구분만 해뒀습니다. 총 71개 커밋.

---

### 2026-09-14 18:53 — 513bf32

베이스라인: 팔꿈치(Pose) 추적 추가 전 상태


---

### 2026-09-14 19:12 — cbe1859

팔 인식 모드 추가 (Pose Landmarker 기반 팔꿈치 굽힘 폐루프 제어)


---

### 2026-09-15 13:14 — 8920336

Initial commit

---

### 2026-09-15 13:19 — 12d5090

merge: GitHub 저장소 초기 커밋과 병합


---

### 2026-09-15 14:29 — d0a39d1

파이썬 프로토타입/펌웨어 폴더 추가 + README 작성


---

### 2026-09-15 15:23 — 536ced3

테스트 모드 추가 (방향키로 채널1/2 즉시 테스트, 안전 최대값/비상정지만 유지)


---

### 2026-09-15 16:10 — 7c89271

테스트 모드: 안전 최대값 클램프·비상정지 상태 체크 제거 (전기 주기+세기 설정만 남김)


---

### 2026-09-15 16:14 — 5880d6a

개인화 모드를 행동 보조 모드 안으로 통합 (모드 4개로 축소, 상호 정지 로직 추가)


---

### 2026-09-15 16:44 — 5aaf11c

팔 인식 모드에 손 굽힘 참고 표시 추가 (팔꿈치+손 동시 측정, 페이지 정리)


---

### 2026-09-15 17:00 — 0e62780

버그 수정: 팔 TARGET/ACTUAL 역할을 매 프레임 x좌표로 재판정하던 걸 고정 판정+체크박스로 변경


---

### 2026-09-15 17:04 — 7290955

버그 수정: 테스트 모드 방향키 반복입력이 시리얼 큐에 쌓여 정지가 늦어지던 문제 (재전송 간격 제한)


---

### 2026-09-15 17:09 — 58e10a2

팔 인식 모드: 손(채널1)/팔꿈치(채널2) 동시 독립 폐루프로 확장 (부위별 채널 분리)


---

### 2026-09-15 17:13 — ae483a1

버그 수정: 팔 인식 모드 좌우 반전(손처럼 미러링 화면에서 기본적으로 뒤집혀 인식됨)


---

### 2026-09-15 17:18 — d974141

비상정지 전수 점검: safetyTripped를 트립 순간만이 아니라 매 tick 확인하도록 수정 (거울모드/팔인식모드/개인화캘리브레이션) + 팔 인식 모드 과도굽힘 시 세기 감소 기능 추가


---

### 2026-09-15 17:25 — 71f4309

속도 조정: 오르내리는 속도를 동일하게 통일하고 30%p 감속 + 팔인식모드 프레임마다 갱신되던 속도 제한 버그 수정 + localStorage에 안 쓰이는 control 값 고정되던 문제 수정


---

### 2026-09-15 17:28 — 7070ce4

속도 재조정: 원래값과 30% 감속값의 중간으로 (kpUp/Down=0.017, maxStep=0.85)


---

### 2026-09-15 17:31 — 6e1d22e

속도 3배로 상향 (kpUp/Down=0.051, maxStep=2.55)


---

### 2026-09-15 17:39 — 26131b9

행동 보조 모드를 목표 %(카메라 폐루프) 자동 세팅 방식으로 전환

- 수저 들기 보조/이두운동: 채널1/2 전류 직접입력 -> 손/팔꿈치 목표 % 입력으로 변경
- 팔 인식 모드의 stepFlexOnlyAxis/driveArmHardwareIfNeeded 폐루프 엔진 재사용
- 이두운동: 굽힘/이완 반복이 고정 시간표 대신 실제 목표 도달(LOCKED) 기준으로 자동 진행
- 시작 전 손+팔 캘리브레이션 확인, 팔 인식 모델 자동 로딩
- 회원 개인화 저장값을 채널 전류 -> 목표 %로 변경
- 행동 보조 모드에서도 필요할 때 Pose 모델 실행되도록 렌더루프 조건 확장
- (버그) 개인화 모드(수동 램프업)가 삭제된 driveActionChannels를 호출하던 걸 발견해 같이 수정


---

### 2026-09-15 17:44 — 1a9a1df

버그 수정: 팔 인식 모드 하드웨어 재전송 주기가 TTL보다 길어서 자극이 끊겼다 이어졌다 하던 문제 (600ms로 단축) + 속도 2배 상향


---

### 2026-09-15 18:06 — 98feea3

팔 인식 모드 속도/유지 버그 수정, 수저 들기 순차 진행, 전 모드 팔 인식 확대

- 세기 증가/감소가 큰 폭으로 "점프"해 보이던 문제를 고쳤다: maxStepUp/
  maxStepDown을 5.1 -> 1로 낮춰 진짜 1씩 점진적으로 오르게 하고,
  controlPeriodMs를 6000 -> 2250(실제 주기 900ms)으로 조정해 목표 도달
  시간이 대략 예전의 2배가 되도록 맞췄다.
- 목표에 도달해 LOCKED로 계속 유지하는 상태가 config.safety.
  maxContinuousStimSeconds(연속 자극 제한, 기본 10초)에 그대로 걸려 "몇 초
  뒤 혼자 꺼짐"으로 보이던 문제 수정 -- 기본값을 60초로 올리고, 이미
  localStorage에 저장된 옛 기본값(10초 이하)도 새 기본값으로 마이그레이션.
- 행동 보조 모드의 "수저 들기 보조"를 손(채널1)+팔꿈치(채널2) 동시 유지
  방식에서, 손이 먼저 목표에 도달하면 전류를 멈추고 팔꿈치(채널2)로
  진행하라고 안내하는 순차 진행 방식으로 변경 (이두운동은 손을 운동 내내
  계속 쥐고 있어야 해서 기존 동시 유지 방식 그대로 둠).
- "거울 모드 제외 모든 화면에서 팔 인식 가능하게" 요청 반영: renderLoop의
  Pose 모델 실행 조건에 테스트 모드를 추가(테스트 모드 진입 시 모델 미리
  로딩도 추가). 테스트 모드의 방향키 전류 출력 자체는 카메라와 무관하게
  그대로 동작하고, 카메라를 켜두면 손/팔 스켈레톤이 참고용으로 화면에
  같이 표시되는 것만 추가됨.
- 행동 보조 모드가 이미 폐루프(자동 세팅)로 바뀐 뒤에도 모드 설명 문구가
  옛 오픈루프(고정 전류 직접 입력) 시절 설명 그대로 남아있던 것을 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-15 18:09 — 4f5eb75

행동 보조 모드에서 팔 인식이 안 되던 문제 수정

renderLoop의 Pose 실행 조건이 행동 보조 모드에 한해서만
"runningActionKey !== null"(실행 버튼을 눌러야만) 켜지게 되어 있어서,
화면에 들어가기만 하고 아직 실행 전이면 팔 인식이 전혀 안 됐다
(테스트 모드는 appMode 조건 하나뿐이라 바로 됐음). 거울 모드를 제외한
모든 모드에서 화면 진입 즉시 Pose가 켜지도록 통일 -- 카메라 시작 시
모델을 미리 불러오는 조건, setAppMode의 사전 로딩 조건도 함께 맞춤.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-15 18:16 — 1f39681

수저 들기 보조: 팔꿈치 목표 도달해도 전류가 안 멈추던 버그 수정

spoonLiftTick의 마지막 단계("elbow" LOCKED -> "done")에서 phase만
바꾸고 더 이상 stepFlexOnlyAxis를 호출하지 않게 해뒀는데, 그러면
actionElbowCtrl.intensity가 마지막 계산값에 "그대로 얼어붙은 채" 계속
하드웨어로 재전송됐다 -- 목표만큼 구부러졌는데도 전류가 안 멈추던
원인. 팔꿈치까지 목표에 도달(LOCKED)하면 실제로 stopActionMode()를
불러 두 채널 다 0으로 정지하고 행동을 완료 처리하도록 수정 (이두운동의
마지막 반복 완료 처리와 동일한 방식으로 통일).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-15 21:42 — 2161cad

개인화 측정(수동 램프업)을 안전 최대값 설정 도구로 재연결

수저/이두운동이 폐루프(목표%)로 바뀐 뒤로 이 측정값이 화면에만 표시되고
어디에도 안 쓰이던 문제를 "재사용"으로 해결. A키로 멈춰서 잰 세기를
"🛡 이 값을 안전 최대값으로 설정" 버튼으로 config.safety.maxIntensity(③
카드)에 바로 채워 넣을 수 있게 함 -- 폐루프는 카메라가 목표%에 도달할
때까지 세기를 계속 올리기만 할 뿐 그 사람이 아파하는지는 모르므로, 미리
사람마다 견딜 수 있는 한계치를 재서 상한으로 걸어두는 용도.

카드 안내 문구도 옛 오픈루프 시절("채널값을 얼마로 넣어야 할지") 그대로
남아있던 걸 새 용도에 맞게 같이 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-15 22:14 — aa61147

테스트 모드: 채널1/채널2 세기를 따로 설정 가능하게 변경

기존엔 testIntensityInput 하나를 두 채널이 같이 썼는데, ←(채널1)/→(채널2)
세기를 각각 입력하는 별도 칸(testIntensityCh1Input/testIntensityCh2Input)으로
나눴다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-15 22:16 — 6b93846

테스트 모드: 방향키를 누르고 있는 동안 전류가 주기적으로 끊기던 버그 수정

기존엔 브라우저의 keydown "자동 반복" 이벤트가 올 때마다 재전송하는
방식이었는데, 이 반복 간격이 OS/브라우저 설정에 따라 들쭉날쭉해서 TTL
(600ms)보다 늦게 오는 경우 그 사이 잠깐씩 전류가 끊겼다. keydown은 눌린
첫 순간에만 반응하고, 그때부터 별도 setInterval(TEST_RESEND_MS=200ms)이
키를 뗄 때까지 계속 재전송하도록 바꿔서 OS/브라우저의 키 반복 타이밍과
완전히 무관하게 끊김 없이 나가도록 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-15 22:20 — ec77722

테스트 모드: 재전송이 시리얼 큐에 밀려 주기적으로 끊기던 진짜 원인 수정

키 반복 타이밍과 무관하게 만든 지난 수정 뒤에도 여전히 "왔다 안 왔다"
하던 원인을 다시 봄 -- TEST_RESEND_MS(200ms)마다 무조건 새로 SET을
보내고 있었는데, SET 하나가 실제로 응답(OK/ERROR)받기까지 그보다 오래
걸리면(직렬 통신 왕복 지연 등) WebSerialLink의 명령 큐(_chain)에 다음
SET이 계속 쌓인다. 큐가 밀리면 실제 도착 시점이 점점 늦어져서 앞서 보낸
SET의 TTL이 먼저 만료돼 채널이 꺼졌다가, 밀려있던 다음 SET이 뒤늦게
도착해 다시 켜지는 식으로 반복된 것.

이전 전송이 아직 응답을 못 받은 상태면 이번 재전송 틱은 새로 쌓지 않고
건너뛰도록(testPulseInFlight 가드) 수정 -- 큐가 절대 밀리지 않고, 실제
재전송 간격이 통신 왕복 속도에 자연스럽게 맞춰진다. TTL도 600ms ->
1000ms로 늘려 여유를 더 줌.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-15 22:37 — 26837cc

행동 보조 모드 개인화 측정 대대적 개편: 수동 램프업 -> 목표 도달 세기 자동 탐색

회원번호 입력 섹션 밑을 다음 순서로 재구성:
  1) 🥄 수저 보조 / 💪 이두운동 선택 버튼 2개 (지금 측정 결과를 어느 행동에
     저장할지 고르는 용도)
  2) 손 목표 %, 팔꿈치 목표 % 입력칸
  3) ▶ 개인화 측정 시작 버튼

시작하면 spoonLiftTick과 같은 원리로 손 -> 팔꿈치 순서로 전류를 자동으로
올려서, 각각 목표 ±5% 안에 들어오는 순간 전류를 멈추고 그때의 세기를
기록한다(손 기록 후 이어서 팔꿈치 측정). 회원번호가 입력돼 있으면 그
회원의 해당 행동(spoonLift/bicep) 기록에 목표%와 찾은 세기를 저장한다.

기존 "🎚 개인화 측정 (수동 램프업 -- A키로 정지)" 도구는 이 새 자동
버전으로 완전히 대체(제거)했다. 거기서 나온 부산물이던 "이 값을 안전
최대값으로 설정" 기능도 함께 제거됨 -- 필요하면 별도로 다시 만들 수 있음.

실행 버튼(수저 들기 보조/이두운동 실행)은 이제 회원의 저장된 세기를
웜스타트로 사용 -- resetActionAxisCtrls() 직후 seedActionAxisCtrlsFromMember()
로 actionHandCtrl/actionElbowCtrl.intensity를 그 값으로 채워서, 매번 0부터
다시 찾지 않고 그 세기부터 시작해 더 빠르게 목표에 도달한다. 각 실행 섹션
아래 "저장된 세기" 안내 문구(spoonLiftSeedText/bicepSeedText)로 지금 어떤
세기가 적용될지 미리 보여준다.

registerMember()가 목표%만 저장하면서 이미 찾아둔 handIntensity/
elbowIntensity를 같이 날려버리던 문제도 같이 고쳐서(기존 값을 읽어와
그대로 이어감), loadMember()도 개인화 측정 선택 상태와 저장된 세기
안내문을 함께 갱신하도록 했다.

행동 보조 모드 카드 상단 소개 문구가 옛 오픈루프 시절 설명("채널1/채널2에
직접 입력한 전류값을 그대로 내보냄") 그대로 남아있던 것도 이번에 같이
수정.

편집 후 node --check, index.html 태그 짝, id 교차 참조(HTML<->JS)까지
전부 재확인.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-15 22:52 — 48e8d88

왼팔/오른팔 구부림 % 비대칭 버그 수정 -- 양팔 각자 보정값으로 분리

같은 각도만큼 구부려도 왼팔 %가 오른팔보다 낮게 나오던 원인: 팔
초기값 측정(펴짐/구부림)이 오른팔(actual)만 재고, 그 하나의 각도
범위를 왼팔(source)에도 그대로 재사용하고 있었다(카메라 각도/팔
길이 차이 등으로 두 팔이 실제로는 다른 각도 범위를 가짐). 이 값은
단순 표시용이 아니라 팔 인식 모드에서 왼팔 %가 오른팔의 실제 목표
(TARGET)로 그대로 쓰이므로, 표시 오차가 아니라 목표 자체가 틀어지는
문제였다.

팔 초기값 측정 시 양팔을 동시에 재서 각자 보정값(calibration.*.Arm=
오른팔, calibration.*.ArmSource=왼팔 신설)을 따로 만들도록 수정.
캘리브레이션 자세(양팔 쫙 펴짐/양팔 최대 구부림)는 원래도 양팔을
대칭으로 유지하는 게 자연스러워서 버튼/절차 추가 없이 그대로 동작.

부수적으로, armSampling 상태 구조를 { sum, count }에서 역할별
{ sums, counts }로 바꾸면서 옛 구조를 참조하던 타임아웃 중단 조건
(armSampling.count)이 그대로 남아있던 것도 발견해 같이 고침 -- 안
고쳤으면 한쪽 팔만 안 보여도 측정이 영원히 안 끝나는 새 버그가 됐을 것.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 18:42 — 7773ee8

테스트 모드에 남아있던 "안전 정지 상태" 표시 제거

원인: 모드를 바꿀 때마다 호출되는 forceZeroController()가 정리 목적으로
공용 상태 표시(controlState)를 항상 "SAFETY_STOP"으로 남겨두는데,
거울/팔인식/행동보조 모드는 다음 tick에서 자기 상태로 바로 덮어써서
문제가 안 됐지만, 테스트 모드는 controlState를 아예 안 건드리는 모드라
이 값이 그대로 남아 실제로는 안전장치가 없는 모드인데도 "안전 정지
상태"가 계속 표시되고 있었다.

테스트 모드로 들어갈 때 이 표시를 바로 대기중으로 되돌리고(카메라가
꺼져 있어도 즉시 반영), 안전 정지 배너(safetyTripRow)도 테스트 모드에
있는 동안은 다른 모드에서 트립된 게 남아있어도 띄우지 않도록 함 --
testModeKeyDown/sendTestPulse는 원래도 safetyTripped를 전혀 참조하지
않으므로 표시만 실제 동작과 안 맞았던 것.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 19:32 — 186a3c0

발표용: 팔 인식 모드 주요 숫자(TARGET/ACTUAL/오차/EMS 제어값) 크게 표시

.compare-value 24px -> 38px, .stim-value 32px -> 48px, .compare-label
11.5px -> 14px, .state-line 13px -> 16px로 확대(공용 CSS라 팔 인식
모드 외 다른 모드에도 동일하게 적용됨). 팔 인식 모드의 손(채널1) 섹션에
따로 걸려있던 더 작은 인라인 크기(20px/22px)도 제거해서 같이 커지게
했고, 손 상태 텍스트도 20px로 키움.

곁다리로, 안내 문구에 남아있던 "연속 자극 시간 제한(기본 10초)"도
지난번에 기본값을 60초로 올린 뒤로 안 고쳐져 있던 걸 발견해 같이 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 19:34 — 33390d2

발표용: "손가락별 목표 vs 실제" 표 화면에서 숨김

카드 자체를 지우면 관련 JS(updateFingerTable 등)가 참조하는 DOM이
없어져 오류가 날 수 있어, 안전하게 display:none으로 화면에서만
숨김(내부 계산/저장은 그대로 계속됨 -- 필요하면 언제든 다시 보이게
되돌릴 수 있음).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 19:40 — 23e8467

회원 개인화 섹션에 "저장" 버튼 추가 (불러오기/회원 등록 옆)

registerMember()가 원래도 새 회원 등록과 기존 회원 값 갱신을 둘 다
처리하는 upsert라서, 새 로직 없이 같은 함수를 호출하는 버튼만 하나
더 추가했다 -- 이름을 몰라 헷갈리지 않게 registerMember(btn)이 실제로
눌린 버튼을 받아 그 버튼에 "저장됨!" 플래시가 뜨도록 함께 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 19:54 — 5ca62a3

발표용: 행동 보조 모드(수저/이두운동)도 팔인식 모드처럼 큰 숫자로 표시

지금까지 수저 들기 보조/이두운동은 손·팔꿈치 상태를 "INCREASING (12)"
같은 작은 초록색 한 줄(state-line)로만 보여줘서, 손 쪽은 그나마 ⑤
카드의 큰 ACTUAL 숫자(항상 손 평균이 표시됨)로 커버됐지만 팔꿈치 쪽은
작은 글씨로만 보였다. 팔 인식 모드의 손(채널1) 섹션과 똑같은 스타일
(TARGET/ACTUAL/오차 큰 칸 + EMS 세기 칸)을 손·팔꿈치 양쪽에 다 추가.

중복 코드를 없애려고 팔 인식 모드에서 손코딩돼 있던 TARGET/ACTUAL/오차
계산을 updateCompareBoxes() 공용 함수로 뽑아내고, 수저 들기 보조/
이두운동의 손·팔꿈치 4곳에서 재사용. 이두운동은 팔꿈치 목표가
굽힘/이완 두 개라 지금 단계(curl/hold=굽힘, release=이완) 기준으로
표시.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 19:59 — c808432

모드 선택에서 팔 인식 모드/행동 보조 모드 버튼 순서 교체

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 20:08 — 046fed2

수저 들기 보조/이두운동 실행 버튼을 나란히 배치하고, 숫자 표시를 4개->2개로 통합

두 행동이 동시에 돌 수 없는데도(runningActionKey 하나뿐) 각자 따로
손/팔꿈치 TARGET·ACTUAL·오차·세기 칸을 갖고 있어서 화면에 항상 4벌이
같이 보였다 -- 실행 안 하는 쪽은 계속 "-"만 떠 있어서 어느 게 실제
값인지 헷갈리기 쉬웠다("수치가 안 나온다"는 것도 이 혼란 때문이었을
가능성이 큼). "손" 1벌, "팔꿈치" 1벌만 공용으로 두고, 지금 실행 중인
쪽(spoonLiftTick 또는 bicepClosedLoopTick)이 그때그때 채워쓰도록
통합했다(actionHand*/actionElbow* 이름으로 정리). 두 실행 버튼도
같은 줄에 나란히 배치.

각 행동 고유의 목표 % 입력칸(spoonLift/bicep)과 "저장된 세기" 안내
문구는 행동별로 다른 값이라 그대로 각자 유지.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 20:13 — cf8498d

팔 초기값 측정을 ② 카드 바로 다음으로 옮김 (③ 안전 설정보다 앞)

기존엔 팔 인식 모드 카드 안(③ 안전 설정보다도 뒤, 손 굽힘 표시 섹션
다음)에 있어서, 손가락 초기값 측정 직후 이어서 팔 초기값까지 측정하려면
안전 설정 카드를 거쳐 스크롤해야 했다. 팔 초기값 측정을 독립된 카드로
빼서 ② 초기값 측정(손가락) 바로 다음, ③ 안전 설정보다 앞에 배치.

행동 보조 모드도 이 값이 있어야 실행되므로(actionCalibrationReady),
이 카드는 팔 인식 모드뿐 아니라 행동 보조 모드에서도 보이게 함 --
기존엔 행동 보조 모드에서 이 값이 없으면 어디서 측정해야 하는지
안내만 있고 카드 자체는 팔 인식 모드에 가야만 보였던 것도 같이 해결됨.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 20:15 — 40e7748

행동 보조 모드에서 ⑤ 카드의 TARGET/ACTUAL/제어 패널 숨김

이 패널(TARGET/ACTUAL/오차 + 제어 시작/해제 버튼 + 판단 상태)은 거울
모드/팔 인식 모드가 실제로 쓰는 공용 controlEnabled 플로우를 보여주는
건데, 행동 보조 모드는 자기만의 실행 버튼/폐루프를 따로 쓰고 이
플로우를 전혀 안 써서 행동 보조 모드에서는 항상 대기중/0으로 고정돼
있었다. 방금 추가한 손/팔꿈치 큰 칸과 헷갈릴 수 있어 행동 보조 모드일
땐 이 패널만 숨기고(기록 로그는 모든 모드 공용이라 계속 보여줌).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 20:48 — fa894ee

수저 들기 보조: 손/팔꿈치 동시 진행으로 되돌리고, 전체 속도 3배 증가

손이 먼저 목표에 도달해야 팔꿈치로 넘어가던 순차 진행을, 실제 수저
드는 동작은 손을 쥐는 것과 팔꿈치를 드는 게 동시에 일어나야 자연스럽다는
피드백에 따라 다시 동시 진행으로 되돌림 -- 손/팔꿈치 둘 다 매 tick
목표를 향해 같이 움직이고, 각자 도달하면 그 축만 유지하다가 둘 다
도달하면 완료 처리(정지)한다. "도달해도 전류 안 멈추던" 이전 버그와
같은 종류의 문제가 재발하지 않게, 완료 조건도 그때와 동일한 방식으로
처리.

속도는 config.control.controlPeriodMs를 2250 -> 750(실제 주기
900ms -> 300ms)으로 줄여서 3배 빠르게 함 -- maxStep(1씩 점진적으로
오르는 것)은 그대로 둬서 "부드럽게 세는" 느낌은 유지. 이 값이 전역
설정이라 팔 인식 모드/이두운동에도 동일하게 적용됨.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 21:06 — 19909e2

테스트 모드 채널1/채널2 세기 기본값을 100으로 변경

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 21:50 — 13139f8

CSV 다운로드를 시행(trial) 기준 요약으로 재설계

예전엔 카메라 프레임마다 한 줄씩(수천 줄) 그대로 CSV로 내보내서 사람이
"몇 번 시도해서 몇 번 성공했는지" 알아보기 어려웠다. 이제 목표에
도달(LOCKED)할 때마다 1회 성공으로 기록하고, 12초 안에 도달 못하면
그 시도를 실패로 기록한 뒤 바로 다음 시도 관찰을 시작한다
(updateTrialTracking, controlEnabled로 실제 목표를 쫓는 동안만 관찰 --
거울 모드/팔 인식 모드에서 동작, 둘 다 공용 controlState를 씀).

CSV에는 맨 위에 요약(총 시행/성공/실패/성공률/평균 도달 시간)을 넣고,
그 아래 시행별 결과(번호/성공-실패/도달 시간/목표%/실제%/시각)를 한
줄씩, 그 아래에 참고용으로 원본 tick 로그를 이어 붙인다. 엑셀에서 한글
안 깨지게 BOM도 추가. "데이터 기록" 카드에 도달 시도/성공 실시간
카운터도 추가.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 21:50 — 6164db2

"개인화 캘리브레이션 (선택)" 카드 화면에서 숨김

관련 JS가 참조하는 DOM을 남겨두기 위해 완전히 지우지 않고
display:none으로 숨김 (손가락별 목표 vs 실제 표를 숨긴 것과 동일한 방식).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 21:54 — ea9218c

행동 보조 모드: 회원번호 시스템 제거, 설정을 컴퓨터 공용으로 저장

회원번호별로 목표%를 따로 저장하던 방식(loadMembers/saveMembers,
emsWebMembers)을 없애고, 이 컴퓨터에서 마지막으로 저장한 값 하나만
기억하는 방식(loadActionSettings/saveActionSettings, emsWebActionSettings)
으로 바꿨다. 수저 들기 보조/이두운동 각 섹션에 자기 목표%(/반복 횟수)만
저장하는 "저장" 버튼을 추가(saveActionInputs) -- 페이지를 새로 열면
applyStoredActionSettings()가 자동으로 다시 채운다.

개인화 측정(finishPersonalizationCal)도 이제 회원번호 없이 바로 이
저장소에 handIntensity/elbowIntensity를 기록하고, 실행 시 웜스타트로
쓰는 seedActionAxisCtrls(구 seedActionAxisCtrlsFromMember)도 같은
저장소를 본다. 회원번호/이름 입력칸, 불러오기/등록 버튼, 관련 안내
문구를 전부 제거.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 21:57 — b1d4860

거울 모드: 컵 잡기/따르기/흔들기 등 미리 정의된 행동 버튼 제거 + 실시간 왼손 연동 타겟 표시 버그 수정

- 컵 잡기/따르기 동작/흔들기 동작/팔 굽히기/물건 놓기 버튼(ACTIONS 배열,
  buildActionButtons/selectAction)을 제거. 전부 실측 데이터가 아닌
  placeholder 목표값이었고, 거울 모드는 실시간 왼손 연동 하나로 충분하다고
  판단. selectedActionText/actionDescriptionText는 실시간 왼손 연동
  전용 상태 표시로 재활용(대기 중 / 연동 중).

- ⚠ 버그 수정: "실시간 왼손 연동 시작"을 눌러도 targetPercent 갱신
  (updateLiveMirrorTarget)이 controlEnabled(=▶ 제어 시작을 누른 상태)
  분기 안에서만 실행되고 있어서, 연동을 시작해도 제어 시작까지 눌러야
  TARGET 값이 나왔다. 실제 전기자극은 여전히 제어 시작을 눌러야 나가는
  게 맞지만, 목표값 표시는 연동 시작 즉시 나오도록 controlEnabled=false
  분기에서도 updateLiveMirrorTarget()을 호출하게 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 21:58 — 7aabfde

행동 보조 모드: 이두운동/수저 들기 보조 섹션·버튼 순서 교체

id는 그대로 두고 화면에 나타나는 순서만 바꿔서(이두운동 -> 수저 들기
보조, 실행 버튼도 이두운동 -> 수저 들기 보조 순), 기능은 전혀 손대지
않음.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 22:05 — cbc2278

음성 명령을 "탱글이" 이름 부르는 항상 켜짐 방식 음성 비서로 재설계

기존엔 버튼 누를 때마다 한 문장만 인식하고 "수저들기보조"/"이두운동"
정확히 두 단어만 알아들었다. 이제 마이크 버튼을 한 번 켜면 계속 듣는
상태가 되고(continuous 인식 + onend에서 자동 재시작), "탱글아"/
"탱그라"라고 부르면 "네"라고 대답한다.

이름을 부른 뒤 10초(VOICE_AWAKE_WINDOW_MS) 동안만 명령을 받아들이는
"깨어있는 시간" 개념을 추가 -- 실제 사람 몸에 전기자극을 내보내는
기능이라, 이름을 부르지 않은 채 흘러가는 일상 대화(예: "나 밥 먹었어")
에 그냥 반응하면 안전하지 않다고 판단해서 넣은 안전장치. 사용자가 물어본
그대로("나 밥먹고싶어", "나 밥먹는거 도와줘", "수저들기 보조해줘" 등)는
전부 인식하되, 이 안전 게이트 뒤에서만 동작한다.

- "수저들기 보조" 계열 문구 -> 지금 앱이 어떤 모드에 있든 행동 보조
  모드로 전환 후 수저 들기 보조 바로 실행.
- "이두운동해줘" -> "몇 회 하시겠어요?"로 되묻고, 숫자(아라비아 숫자
  또는 한글 숫자 1~10)로 답하면 그 반복 횟수로 설정해서 바로 실행.

버튼은 그대로 두되 라벨/의미를 "마이크 켜기/끄기"(항상 듣기 온오프)로
바꿈. 관련 HTML 안내 문구도 새 동작에 맞게 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 23:21 — fefe99d

음성 비서: 항상 듣기 자동 시작 + 오른쪽 위 상태 표시 + AI(Gemini) 자연어 인식

- 페이지를 열면 마이크 버튼을 안 눌러도 자동으로 "항상 듣는" 상태로
  시작한다(toggleVoiceCommand()를 init에서 한 번 호출). 버튼은 끄고
  싶을 때 쓰는 수동 온오프 용도로 남겨둠.
- 화면 오른쪽 위에 항상 떠 있는 작은 표시(voiceListenIndicator) 추가 --
  "듣고 있음" / "탱글이가 듣고 있어요(깨어있음)" / "몇 회인지 대답해주세요"
  상태를 어느 모드/카드에 있든 항상 보여준다. awake 상태가 10초 뒤
  저절로 풀리는 것도 반영되게 0.5초마다 갱신.
- 정해진 문구(수저/숟가락/밥먹고싶/밥먹는거/이두)로 못 잡은 발화는,
  API 키가 입력돼 있으면 Gemini에게 보내 "수저 들기 보조/이두운동/
  둘 다 아님" 중 뭘 원하는지 판단시킨다(classifyVoiceIntentWithAI --
  옛 거울 모드의 interpretCommandWithAI()와 같은 REST 직접 호출 +
  responseSchema로 JSON 강제하는 패턴 재사용). "나 배고파", "팔 운동
  좀 하고싶어" 같은 표현도 이제 인식됨. 음성 비서 카드에 API 키 입력칸
  추가(비어있으면 정해진 문구만 인식, 기존 aiConfig 저장소 재사용).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 23:26 — 3422ca6

버그 수정: 페이지 로딩이 도중에 멈춰서 모드 버튼이 전혀 안 눌리던 문제

원인: 마이크 자동 시작 코드(if (SpeechRecognitionCtor) toggleVoiceCommand();)
가 파일 앞쪽(초기화 구간)에서 실행되는데, toggleVoiceCommand()가 참조하는
voiceEnabled/voiceAwakeUntil/voicePendingBicepReps는 파일 훨씬 뒤쪽(음성
비서 섹션)에 let으로 선언돼 있었다. 이 자동 시작 호출 시점엔 아직 그
선언부에 도달하지 못했으므로 TDZ(Temporal Dead Zone)에 걸려 "Cannot
access before initialization" 오류가 그 자리에서 던져졌고, 이 오류가
잡히지 않은 채로 스크립트 전체 실행이 그 줄에서 멈춰버렸다 -- 그
뒤에 나오는 모드 선택 버튼을 포함한 모든 addEventListener 등록 코드가
실행되지 못해 화면의 모든 버튼이 먹통이 됐던 것.

voiceEnabled/voiceAwakeUntil/voicePendingBicepReps/VOICE_AWAKE_WINDOW_MS
선언을 voiceRecognition과 같이 파일 맨 위(자동 시작 호출보다 앞)로
옮겨서 해결. node --check는 문법만 보고 이런 실행 순서 문제는 못 잡아서
발견이 늦었음 -- 이후 비슷한 자동 실행 코드를 앞쪽에 추가할 때는 거기서
참조하는 let/const가 전부 그보다 앞에 선언돼 있는지 직접 확인 필요.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 23:34 — 2927ee5

이두운동: 수축(굽힘) 완료될 때마다 횟수를 소리로 세줌

굽힘(curl) 단계가 목표에 도달하는 순간을 "1회"로 세고, 유지/이완까지
기다리지 않고 그 즉시 speak()로 "N회"라고 읽어준다(로그/토스트에도 남김).
"수축완료가 1회 기준"이라는 요청대로, 유지+이완이 끝나야 세는 게 아니라
굽힘 자체가 완료되는 시점 기준으로 카운트.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 23:35 — ab10a6a

음성 비서: AI 호출 실패 시 화면 글자뿐 아니라 소리로도 알려줌

Gemini가 503(과부하)/네트워크 오류 등으로 응답 실패하면 이전엔 로그에만
남고 사용자에게는 아무 반응이 없어서 왜 조용한지 알기 어려웠다.
classifyVoiceIntentWithAI가 null(호출 실패)을 반환하면 speak()로도
안내하도록 수정 (AI가 정상적으로 "관련 없음"이라고 판단한 경우와는
구분해서, 실패했을 때만 이 안내가 나감).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 23:39 — b3f7fb9

음성 비서: 한 번 명령 수행 후 "탱글아"를 다시 불러도 반응 없던 문제 수정

브라우저 음성 인식 세션이 (특히 speak()로 응답한 뒤) 조용히 끊기는
경우가 있는데, 그 직후 onend에서 바로 부르는 재시작(start())이 타이밍
문제로 실패하면 그냥 조용히 포기하고 있었다 -- 그러면 voiceEnabled는
여전히 true인데 실제 인식 세션은 죽어있는 상태가 돼서, 마이크를 껐다
켜야만(다시 start() 호출) 원래대로 돌아왔다.

voiceSessionActive(세션이 실제로 살아있는지)를 onstart/onend로 추적하고,
0.5초마다 도는 watchdog(watchVoiceSession)이 "켜져 있어야 하는데 세션이
안 살아있는" 상태를 보면 계속 재시작을 시도하게 만들어 스스로 복구되도록
수정. 정상 작동 중일 땐 watchdog이 아무것도 안 해서 평소엔 조용하다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-16 23:52 — 76877f1

음성 비서: 수저/이두 둘 다 아니면 그냥 대화형 AI처럼 대답하게 확장

classifyVoiceIntentWithAI의 판단 결과에 "chat" 액션을 추가 -- 수저 들기
보조/이두운동과 무관한 말(저녁 메뉴 추천, 잡담, 일반 질문 등)이면 전기
자극은 실행하지 않고 AI가 만든 대답(reply)을 speak()로 그대로 읽어준다.
같은 AI 호출 한 번으로 "행동 실행 판단"과 "일반 대화 응답"을 동시에
처리(액션 분류 스키마에 reply 필드 추가). 대화가 자연스럽게 이어지도록
chat 응답 후에도 깨어있는 시간(10초)을 연장.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 00:29 — d1aa10e

이두운동 굽힘 유지 시간 1초 -> 0.5초로 변경

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 00:34 — 995b13e

팔 인식 정확도 개선: pose 모델 lite -> full, 카메라 해상도 640x480 -> 1280x720(ideal)

두 가지가 팔 인식 각도 오차의 주요 원인으로 보여서 같이 손봤다.
1) PoseLandmarker가 빠르지만 덜 정확한 "lite" 모델을 쓰고 있었다 --
   pose_landmarker_full.task(공식 MediaPipe 모델)를 새로 받아서 교체.
   손 인식(hand_landmarker)과 맞춰 minPoseDetectionConfidence/
   minPosePresenceConfidence/minTrackingConfidence도 0.7로 설정.
2) 카메라 해상도가 640x480이라, 손 하나만 잡을 땐 충분해도 어깨~손목까지
   화면에 다 들어와야 하는 팔 인식에서는 관절 하나하나가 차지하는
   픽셀 수가 너무 적어 각도 오차가 컸다. 1280x720(ideal)로 올림 --
   canvas 크기는 이미 video.videoWidth/videoHeight를 그대로 따라가게
   되어 있어서 추가 수정 없이 자동으로 커짐. 이 해상도를 지원 안 하는
   웹캠이면 자동으로 가능한 해상도로 낮아짐(ideal이라 실패하지 않음).

손 인식은 그대로 hand_landmarker.task(원래도 정확도 높은 모델)를 계속
쓰므로 영향 없음 -- 이번엔 팔 쪽만 정확도를 올림.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 00:37 — b28e0bf

이두운동: 굽힘 유지 단계 완전히 제거 -- 도달하면 바로 이완으로

수축(굽힘) 완료 즉시(유지 없이) 이완 목표로 넘어가도록 변경. 관련
BICEP_CURL_HOLD_MS 상수/holdStartedAt 상태/"hold" 단계를 전부 정리하고
안내 문구도 맞춰 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 00:44 — 06bab09

이두운동: 수축이 목표 이상 되면 LOCKED 기다리지 않고 바로 이완으로 전환

기존엔 굽힘 단계가 허용범위 안에서 successHoldSeconds만큼 버텨야 하는
LOCKED 상태가 돼야 이완으로 넘어갔는데, 이제 실제 굽힘 %가 목표치에
도달하거나 넘어서는 즉시(armLatestPercent.actual >= elbowCurlTarget)
바로 이완으로 전환한다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 00:49 — 969b2b8

이두운동: 이완도 목표 이하 되면 LOCKED 기다리지 않고 바로 다음 단계로

굽힘->이완 전환과 같은 방식으로, 이완 단계도 실제 %가 목표치에 도달하거나
그 이하가 되는 즉시(LOCKED 대기 없이) 다음 반복(굽힘) 또는 완료 처리로
넘어가도록 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 00:53 — cbd4689

이두운동: 지정 횟수 다 끝나면 "완료되었습니다"를 음성으로 안내

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 01:03 — fc19baf

행동 보조 모드에서 "개인화 측정(목표 도달 세기 자동 탐색)" 기능 완전히 제거

요청대로 이 기능 전체(HTML 카드/버튼/입력칸, JS 상태·함수 전부)를 지웠다.
이 기능의 유일한 소비처였던 웜스타트(seedActionAxisCtrls)와 "저장된 세기"
안내 문구(spoonLiftSeedText/bicepSeedText)도 더 이상 채워질 값이 없어져
같이 정리 -- 이제 수저 들기 보조/이두운동 실행은 항상 0부터 자동으로
세기를 찾는다. saveActionInputs/applyStoredActionSettings도 이제 목표%
(/반복 횟수)만 다루도록 단순화.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 01:05 — 18a0768

오른손/오른팔을 카메라가 놓쳤을 때 걸리던 비상정지 제거

runtimeCheck()에서 handLost 조건을 제거 -- 이제 손/팔을 놓쳐도 완전
비상정지는 안 걸린다. 시리얼 연결 끊김/연속 자극 시간 초과/전체 실험
시간 초과는 그대로 비상정지 사유로 남아있음. 손을 놓친 동안은 각 모드
컨트롤러가 WAITING_FOR_HAND로 표시하며 목표 추적만 잠깐 멈추는 기존
동작은 그대로 유지(강제 정지는 아님).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 01:09 — 577e334

음성 비서: "N회" 발음이 어색하던 문제 수정 -- 숫자를 한글로 풀어서 읽게 함

"이두운동 3회를 시작합니다"처럼 숫자+회를 그대로 speak()에 넘기면
브라우저 한국어 TTS가 숫자와 단위 사이를 어색하게 끊어 읽는 경우가
있었다. sinoKoreanNumber()로 숫자를 한글 한자어 수사로 변환해서
("이두운동 삼회를 시작합니다") 훨씬 자연스럽게 읽히도록 수정 -- 시작
안내와 매 회차 카운트 음성 둘 다 적용.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 01:10 — 5bd1847

음성 비서: 일반 대화 응답이 너무 길게 나오던 것 -- 짧은 한 문장으로 제한

"운동을 모두 마치셨군요... 필요하신 게 있으면 말씀해주세요" 처럼 뒤에
인사치레/추가 제안이 붙어 길어지던 문제. 시스템 프롬프트에서 "딱 핵심만
담은 한 문장, 뒤에 덧붙이는 말 금지"로 명확히 제한.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 01:12 — 8144e37

이두운동 횟수 완료 팝업을 5배 크게 표시

showToast에 big 옵션 추가(.toast.big -- 글자 크기 14px -> 70px, 패딩/
테두리도 맞춰서 키움). 다른 토스트(경고/오류 등)는 기존 크기 그대로 두고
이두운동 회차 완료 알림에만 적용. 커진 글자를 읽을 시간을 조금 더 주려고
표시 시간도 1.5초 -> 1.8초로 살짝 늘림.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 01:32 — 033a195

"전체 실험 제한시간" 기능 완전히 제거

③ 카드의 "전체 실험 제한시간 (초)" 입력칸/"⏱ 타이머 초기화" 버튼/남은
시간 표시(pill)와, 그 뒤의 강제 정지 로직(totalTimeExceeded, runtimeCheck/
spoonLiftTick/bicepClosedLoopTick에서의 체크)을 전부 제거. config.safety.
totalExperimentSeconds/experimentStart도 정리.

⑤ 카드의 "경과 시간" 표시는 그대로 남겨뒀다 -- 이건 카메라를 처음 켠
뒤 얼마나 지났는지 보여주기만 하는 참고용이고 아무것도 강제로 멈추지
않아서, 요청하신 "제한시간" 기능과는 별개로 판단(sessionStartedAt으로
이름을 바꿔 그 표시 전용으로만 남김).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 09:52 — c7b5318

손 좌우 뒤집기 체크박스와 팔 좌우 뒤집기 체크박스를 하나로 동기화

카메라가 좌우로 뒤집혀 보이는 원인은 손/팔이 따로가 아니라 카메라 자체
하나라서, 둘 중 아무 체크박스나 누르면 나머지 하나도 같은 상태로 같이
바뀌게(체크박스 표시 + 실제 값 invertHandedness/invertArmSides 둘 다)
동기화. 매번 두 군데 다 따로 체크할 필요 없어짐. 안내 문구도 서로
연결되어 있음을 명시하도록 수정.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 10:26 — 8aeea9c

버그 수정 2건: 목표 0에서도 전류 안 꺼지던 문제 / 테스트 모드 정지 지연 문제

1) stepFlexOnlyAxis(팔 인식/행동 보조 공용)와 updateController(거울 모드)
   둘 다, 목표(targetPercent)가 거의 0인데 실제 굽힘도 마침 그 근처라서
   허용오차 안에 들어오면 "목표 도달 -- 유지" 분기를 타면서 그 순간까지
   남아있던 세기를 그대로 "유지"해버렸다. 목표가 낮아져서 0이 된 상황도
   이 분기로 들어가는데, 그러면 유지할 자세가 없는데도 전류가 안 꺼지는
   문제였다. 목표 자체가 허용오차 이하로 낮으면 세기를 0으로 내리도록 수정.

2) 테스트 모드에서 카메라를 켜둔 채 방향키를 뗐을 때 전류가 바로 안
   끊기던 문제 -- 최근 "거울 모드 제외 전부 팔 인식" 요청과 Pose 모델
   lite->full 교체(+해상도 상향)가 겹치면서, Pose 추론이 메인 스레드를
   붙잡는 시간이 길어져 정지(세기 0) 시리얼 명령이 밀려 늦게 나가는
   원인이 됐다. 테스트 모드는 "즉시 정지"가 핵심이라 팔 인식(참고용
   표시일 뿐이었음)을 이 모드에서만 다시 뺐다 -- 손 스켈레톤은 그대로
   보임. 팔 인식 모드/행동 보조 모드는 그대로 유지.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 10:47 — e674394

음성 비서: 식사 관련 말 들으면 바로 실행하지 않고 "실행할까요?" 되물음

"밥먹고싶어" 등 수저 들기 보조 관련 발화(정해진 문구든 AI 판단이든)를
들으면 바로 실행하지 않고 "수저 들기 보조를 실행할까요?"라고 한 번
되묻는다. "응"/"네"/"예"/"해줘"/"실행"/"시작" 등 긍정으로 대답하면
그때 실행하고, 아니면(또는 시간 초과) 실행하지 않고 취소.
이두운동의 "몇 회 하시겠어요?" 되묻기와 같은 패턴(isAffirmative 헬퍼로
긍정 여부 판단).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 13:34 — ff84c19

fix: 팔 인식/행동 보조 모드에서 목표가 낮아져도 전기가 안 줄어들던 문제 수정

stepFlexOnlyAxis가 세기를 내릴 때도 올릴 때와 똑같이 300ms 주기에 1씩만
내려가서, 실시간으로 계속 바뀌는 목표(왼손/반대팔 연동)가 훅 낮아져도
액추얼이 한동안 목표보다 훨씬 높은 채로 남아 전기가 계속/더 세지는 것처럼
보였다. 세기를 낮추는 건 안전 문제가 없으므로, 내려갈 때는 주기 제한 없이
매 프레임 훨씬 빠르게(reduceKp/reduceMaxStep) 따라가도록 분리했다. 거울모드
updateController()의 kpDown/maxStepDown(신전 채널 게인)은 건드리지 않았다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-17 16:21 — 66d6007

fix: 행동 보조 모드에서 팔 인식 모델 첫 추론 워밍업 비용을 로딩 단계로 이동

pose_landmarker_full.task(+720p)로 정확도를 올린 뒤로 첫 detectForVideo()
호출에서 WASM 추론 커널이 그제서야 준비(JIT)돼 몇 초씩 걸렸는데, 이 비용이
이두운동을 막 시작한 시점의 실제 프레임에 그대로 나가서 "초반엔 팔 엑추얼
값이 측정 안 됨"으로 보였다. ensurePoseLandmarker()가 모델을 만든 직후
빈 캔버스로 한 번 미리 detectForVideo를 호출해 그 워밍업 비용을 로딩
토스트가 떠 있는 준비 단계에서 대신 치르게 했다.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-18 11:29 — 7a46b8d

docs: 최종 시연 문서/케이스 설계/초기 프로토타입 정리 (포트폴리오 이관용)

- 기존 PDF 보고서 3개를 docs/로 이동, 전시판넬 PDF·설계정리 docx·재활보조
  시스템 보고서·시연 시 패치 위치 사진을 docs/에 추가
- 발표 구술 초안을 정리해 docs/시연_대본_및_QA.md 작성 (신경가소성 정의 채워
  넣기, 근거 부족한 ±5% 수치는 일반화, 부정확하다고 지적된 표현은 대체 문구로
  교체, 확인이 필요한 메모는 별도 섹션으로 남김)
- 보드 케이스 3D 프린팅 설계(STL)와 조립 영상을 hardware/case/에 추가
- 현재 구조 이전의 초기 하드웨어 프로토타입(피드백 장갑, 주먹 버튼 테스트)을
  archive/early_prototypes/에 개발 히스토리로 보존
- README의 폴더 구조 표/안전 설정 설명을 지금 코드 기준(4모드, 연속자극
  60초, 전체 실험 제한시간 없음)으로 갱신

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-18 11:39 — e7d114a

docs: 파일별 설명 문서 추가 및 여는 방법 안내 최신화

- 파일_설명.txt 신규 추가: app.js/index.html/*.task 등 최상위 파일과
  docs·hardware·firmware·archive 등 폴더가 각각 무슨 역할인지 비개발자도
  이해할 수 있게 정리
- 열기_안내.txt가 구버전 ems_demo_standalone.html을 "추천 방법"으로
  안내하고 있던 걸, 지금 최종본인 index.html(run_server.bat)이 방법 1이
  되도록 순서를 바꾸고 구버전 한계를 명시

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---

### 2026-09-18 11:41 — 26e34d4

cleanup: 구버전 스냅샷/standalone 데모/미사용 lite 모델 삭제

저장소 목적이 "최종 기록/포트폴리오"로 정해지면서, 실제로는 안 쓰는 구버전
사본들이 계속 헷갈림을 줄 뿐이라는 피드백에 따라 정리:

- mediapipehand/ (2026-08-23 구버전 스냅샷 전체) 삭제
- ems_demo_standalone.html (위 구버전을 파일 하나로 번들링한 데모) 삭제
- pose_landmarker_lite.task (full 모델로 교체된 뒤 코드에서 안 쓰던 파일) 삭제

README.md/파일_설명.txt/열기_안내.txt에서 위 항목을 안내하던 부분도 함께
정리. 필요하면 git 히스토리(예: 이 커밋의 부모)에서 언제든 복원 가능.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


---
